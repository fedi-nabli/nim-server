# Simple Nim server

This is a simple nim server that has two endpoints. The first returns a static string and the second open a file in the docs folder.

This is meant as a simple code to evaluate nim's stdlib http server handling.
